"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.float = exports.int = exports.getRandomFloat = exports.getRandomInt = void 0;
const getDefaults = (options = {}) => {
    const maxSafe = options.maxSafe ?? Number.MAX_SAFE_INTEGER;
    const max = options.max ?? maxSafe;
    const min = options.min ?? maxSafe / 2;
    return {
        max,
        min,
        maxSafe,
    };
};
function getRandomInt(random, options) {
    const { max, min } = getDefaults(options);
    const number = random() * (max - min) + min;
    return ~~number;
}
exports.getRandomInt = getRandomInt;
function getRandomFloat(random, options) {
    const base = Math.round(getRandomInt(random, options));
    const decimal = getRandomInt(random, { min: 0, max: 99 });
    return parseFloat(`${base}.${decimal}`);
}
exports.getRandomFloat = getRandomFloat;
const int = (random, options) => ~~getRandomInt(random, options);
exports.int = int;
const float = (random, options) => getRandomFloat(random, options);
exports.float = float;
